# TODO

- [ ] Locate and remove unresolved git merge conflict markers in `health-companion-ui/src/pages/dashboard.tsx` (and any other affected files).
- [ ] Re-run TypeScript/Next build (local) to ensure `npm run build` passes.
- [ ] Commit-ready verification: confirm no remaining conflict markers in UI source.

